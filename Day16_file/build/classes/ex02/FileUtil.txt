패키지 선언

파일을 다루기 위한 자바 기본 클래스
입출력간에 발생할 수 있는 예외 클래스

JSP 내장 객체 application의 클래스
JSP 내장 객체 request의 클래스

파일첨부를 위한 클래스
파일이름새로지정하기 위한 기본 정책 클래스
파일이름정책 인터페이스

클래스 FileUtil

	싱글톤을 위한 필드
	
	싱글톤 객체를 반환하는 public 메서드
	.
	.
	
	외부에서 접근할 수 없도록 생성자 접근제한자를 private으로 설정
	
	파일을 저장할 디렉토리 이름 (초기값 null)
	파일업로드 시 허용할 최대 용량 (byte)
	파일 및 문자열 인코딩
	파일 이름 재설정 기본 규칙 정책
	
	request를 전달받아서 Ex02DTO 타입으로 객체를 반환하는 함수 getDTO
		비어있는 Ex02DTO 타입의 객체 dto를 하나 생성한다
	
		만약 저장디렉토리가 지정되지 않았으면
			request에서 application을 불러온다
			저장디렉토리는 /upload 로 지정한다
			저장디렉토리를 체크할 파일 객체를 생성한다
			디렉토리가 없으면 새로 생성한다
		.
		
		request와 저장디렉토리, 최대용량, 인코딩, 정책으로 
			MultipartRequest의 객체를 생성한다
		.
		
		// 일반 문자열 파라미터도 mpRequest에서 가져와야 한다
		입력값 중에서 title을 문자열로 가져온다
		입력값 중에서 profileImage를 파일형태로 가져온다
		파일의 저장위치를 upload/ + 파일이름 으로 지정한다
		
		dto에 타이틀을 넣어준다
		dto에 파일저장경로를 넣어준다
		// DB에 파일을 직접 저장하지 않는다 (파일의 이름 및 경로만 저장하면 된다)
		
		만들어진 dto를 반환한다		
	.
	
	저장디렉토리에서 파일의 목록을 문자열의 배열형태로 반환하는 함수 getFileNameList
		만약, 저장디렉토리가 지정되어 있으면
			디렉토리를 파일객체로 생성한 후
			
			대상 디렉토리가 실제로 존재한다면
				디렉토리 내부 파일의 목록을 문자열로 반환한다
				
		저장디렉토리가 지정되어 있지 않으면 null을 반환한다
	.
		
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	